import { CardEditor } from "@/components/editor/card_editor"; // opens the trading card workspace.

export default function Home() { // renders the primary editor.
  return <CardEditor />; // provides the complete card creation flow.
}
